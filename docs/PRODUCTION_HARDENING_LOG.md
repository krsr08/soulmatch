# Production Hardening Log
- Backup branch: backup/pre-production-20260517-200142
- Backup tag: pre-production-backup-20260517
- Working branch: feat/production-hardening
- Started: 2026-05-17 20:01 IST

## Status
| ID | Item | Status | Commit | Notes |
|----|------|--------|--------|-------|
| P1-01 | Secrets & repo hygiene | Done | 78eaa84 | Added GitHub Actions gitleaks scan, verified ignore coverage for env/build artifacts, and scanned tracked files. `android/app/google-services.json` contains a Firebase mobile API key; it is public client config but should be restricted/rotated in Firebase/Google Console before launch. |
| P1-02 | Docker production network | Done | c03066a | Verified Postgres, MongoDB, and Redis are internal-only. Backend service ports are bound to `127.0.0.1` for the VM-local gateway and health checks; only Nginx should be public. Documented this in `docs/DEPLOYMENT.md`. |
| P1-03 | API gateway / reverse proxy | Done | c03066a | Added CORS allowlist support across backend services and matching service, documented single public API base URL, and added OTP IP/phone rate limits. |
| P1-04 | Auth service hardening | Done | 20f40e4 | Added issuer/audience/jti JWT claims, refresh rotation revocation markers, production MOCK_OTP boot guard, OTP IP/phone throttles, and verified Google-email linking. Tested with `node --test backend/auth-service/test/tokenService.test.js`; route limiters enforce 3 OTP/15m per phone and 10/15m per IP. |
| P1-05 | Android release security | Done | c900205 | Confirmed release backup/cleartext/logging guards, added encrypted token storage using Android Keystore-backed encrypted preferences, removed blocking token lookup from the OkHttp auth interceptor, and confirmed FCM token registration/channel/permission path. |
| P1-06 | Remove release mocks | Done | c900205 | Kept demo fixtures debug-only through `AppEnvironment.allowDemoFallback`, blocked mock checkout completion in release builds, and left release API failures as error/empty state instead of fake data. |
| P1-07 | Profile privacy | Done | c900205 | Added shared profile visibility/redaction helpers, fixed null admin-status visibility handling, enforced `matches_only` profile visibility, redacted photo/contact/last-seen fields centrally on profile detail, and changed detail-screen hide to call server match feedback. Tested with `node --test backend/profile-service/test/profileVisibility.test.js`. |
| P1-08 | Search filter parity | Done | 35704b9 | Started `docs/SEARCH_FILTER_MATRIX.md`, implemented exposed filters including height, employment, profile posted by, activity, viewed, nearby, horoscope, manglik, and cap limit to 50. Search responses now include `appliedFilters`. Tested with `node --test backend/search-service/test/searchFilters.test.js`. |
| P1-09 | Matching correctness | Done | pending | Added JWT issuer/audience verification for matching and downstream service auth middleware, capped match page size to 50, added per-user match route rate limiting, pushed partner-preference filters into SQL before scoring, enforced block/privacy/matches-only visibility in matching and compatibility reads, and added compatibility explanations. Tested with `python -m py_compile backend\matching-service\app\middleware\auth.py backend\matching-service\app\routes\matches.py backend\matching-service\app\services\matching_engine.py` and `python -m unittest backend\matching-service\test\test_matching_security.py`. |
| P1-10 | Chat security | Done | pending | Verified message-history route requires requester participation, socket send/typing/read paths validate eligibility/participation, pagination is capped at 100, and JWT socket auth now verifies issuer/audience. Added regression checks in `backend/chat-service/test/chatRouteSecurity.test.js`. |
| P1-11 | Notification lockdown | Done | pending | Confirmed `/send` and `/template` require `x-internal-service-secret`, inbox/read/device registration routes require member JWTs, notifications are persisted before delivery, and invalid FCM tokens are pruned on permanent token errors. Added regression checks in `backend/notification-service/test/notificationSecurity.test.js`. |
| P1-12 | Payments | Done | pending | Verified orders are persisted before returning Razorpay order IDs, client verify and webhook paths reconcile order/amount/currency/plan, idempotent paid orders return the existing subscription, and production now rejects missing or placeholder Razorpay credentials. Tested with `node --test backend/payment-service/test/paymentController.test.js`. |
| P1-13 | Admin auth & RBAC | Done | pending | Added admin role and permission tables, issuer/audience checked admin JWTs, httpOnly admin session cookie support, logout cookie clearing with audit, 5-failure login lockout, optional TOTP enforcement via `ADMIN_MFA_TOTP_SECRET`, role-table permission loading, and restricted admin realtime CORS. Tested with `node --test backend/admin-service/test/adminSecurity.test.js`. |
